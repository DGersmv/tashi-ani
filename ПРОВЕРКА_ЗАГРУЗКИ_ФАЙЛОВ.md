# 📁 Проверка загрузки файлов на сервер

## ✅ Хорошие новости

1. **DNS обновился** - все A записи указывают на `31.31.196.254` ✓
2. **SSL сертификат получен** - "Let's Encrypt сертификат tashi-ani.ru_le3 получен успешно" ✓
3. **Сайт открывается** - но показывает старую версию

## ❌ Проблема

Сайт открывается, но показывает **старую версию** (старый сайт), а не новый из папки `dist`.

## 🔍 Что проверить через SSH

Выполните в SSH:

```bash
# 1. Найти папку сайта
echo "Проверяю возможные папки:"
ls -la ~/public_html/ 2>/dev/null && echo "✓ Найден public_html"
ls -la ~/www/ 2>/dev/null && echo "✓ Найден www"
ls -la /var/www/html/ 2>/dev/null && echo "✓ Найден /var/www/html"
ls -la ~/domains/tashi-ani.ru/public_html/ 2>/dev/null && echo "✓ Найден domains/tashi-ani.ru/public_html"

# 2. Проверить, есть ли index.html в корне
find ~ -name "index.html" -type f 2>/dev/null | head -10

# 3. Проверить, есть ли папка api с PHP файлами
find ~ -path "*/api/db.php" -type f 2>/dev/null | head -5

# 4. Проверить, есть ли config.php
find ~ -name "config.php" -type f 2>/dev/null | head -5
```

## 📤 Как загрузить файлы через FileZilla

### Шаг 1: Подключение

1. Откройте **FileZilla**
2. **Новое подключение:**
   - Хост: `server257.hosting.reg.ru` или `31.31.196.254`
   - Пользователь: `u3269198`
   - Пароль: (ваш пароль от хостинга)
   - Порт: `22` (SFTP) или `21` (FTP)
   - Протокол: **SFTP - SSH File Transfer Protocol**

3. Нажмите **"Быстрое подключение"**

### Шаг 2: Найти папку сайта

Обычно это одна из папок:
- `/home/u3269198/public_html/`
- `/home/u3269198/www/`
- `/home/u3269198/domains/tashi-ani.ru/public_html/`

**Как найти:**
- Посмотрите, где находится текущий `index.html` (старый сайт)
- Это и есть папка сайта

### Шаг 3: Загрузить файлы

1. **В FileZilla:**
   - Слева: откройте папку `E:\tashi-ani\dist`
   - Справа: перейдите в папку сайта на сервере

2. **Выберите ВСЕ файлы и папки** из `dist`:
   - `index.html`
   - `api/` (вся папка)
   - `_next/` (вся папка)
   - `public/` (вся папка)
   - `config.php`
   - `.htaccess`
   - Все остальные файлы

3. **Перетащите** всё в папку сайта на сервере

4. **При замене файлов** - выберите "Перезаписать"

### Шаг 4: Проверить права доступа

После загрузки в SSH:

```bash
cd ~/public_html  # или где ваш сайт

# Установить права
chmod 755 .
chmod 644 *.html *.php
chmod 755 api
find api -type f -exec chmod 644 {} \;
find api -type d -exec chmod 755 {} \;
chmod 644 .htaccess
chmod 644 config.php
```

## ✅ Проверка после загрузки

1. **Проверьте в браузере:**
   - Откройте `http://tashi-ani.ru` (очистите кэш: Ctrl+F5)
   - Должен открыться новый сайт

2. **Проверьте API:**
   - Откройте `http://tashi-ani.ru/api/portfolio.php`
   - Должен вернуться JSON

3. **Проверьте через SSH:**
   ```bash
   ls -la ~/public_html/index.html
   ls -la ~/public_html/api/db.php
   ls -la ~/public_html/config.php
   ```

## 🔄 Очистка кэша браузера

Если сайт не обновился:

1. **Очистите кэш браузера:**
   - Chrome/Edge: `Ctrl + Shift + Delete`
   - Или: `Ctrl + F5` (жесткая перезагрузка)

2. **Проверьте в режиме инкогнито:**
   - Откройте сайт в режиме инкогнито
   - Это покажет актуальную версию без кэша

## 📋 Чек-лист

- [ ] Найдена папка сайта на сервере
- [ ] Загружены все файлы из `dist`
- [ ] Установлены правильные права доступа (755/644)
- [ ] Проверен `index.html` в корне сайта
- [ ] Проверена папка `api/` с PHP файлами
- [ ] Проверен `config.php`
- [ ] Очищен кэш браузера
- [ ] Сайт открывается с новым содержимым

---

**Выполните команды из раздела "Что проверить через SSH" и пришлите результаты!**
