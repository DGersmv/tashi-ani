# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ НА СЕРВЕРЕ

## Проблема
Next.js запускается на внешнем адресе `192.168.0.2:3000` вместо `127.0.0.1:3000`, что делает его доступным напрямую и уязвимым для атак.

## Решение

Выполните на сервере:

```bash
cd /var/www/tashi-ani

# 1. Обновите package.json
git pull origin master

# ИЛИ вручную исправьте start скрипт:
# Откройте package.json и измените:
# "start": "next start"
# на:
# "start": "next start -H 127.0.0.1 -p 3000"

# 2. Остановите текущий процесс
pm2 delete tashi-ani

# 3. Запустите заново с правильными параметрами
pm2 start npm --name "tashi-ani" -- start
pm2 save

# 4. Проверьте что слушает только localhost
sleep 2
sudo netstat -tulpn | grep :3000
# Должно быть: 127.0.0.1:3000 или ::1:3000
# НЕ должно быть: 0.0.0.0:3000 или 192.168.x.x:3000

# 5. Проверьте логи
pm2 logs tashi-ani --lines 10 --nostream
# Должно быть: "Local: http://127.0.0.1:3000"
# НЕ должно быть: "Network: http://192.168.x.x:3000"

# 6. Проверьте доступность через nginx
curl -I https://tashi-ani.ru
```

## Проверка безопасности

После исправления проверьте:

```bash
# 1. Next.js должен быть доступен ТОЛЬКО через nginx
curl http://127.0.0.1:3000
# Должно работать

# 2. Прямой доступ снаружи должен быть заблокирован
# (если у вас есть доступ с другого сервера)
# curl http://IP_СЕРВЕРА:3000
# Должно НЕ работать (таймаут или отказ)

# 3. Через nginx должно работать
curl https://tashi-ani.ru
# Должно работать
```

## Важно

После исправления:
- ✅ Next.js будет слушать только `127.0.0.1:3000`
- ✅ Доступ будет только через nginx
- ✅ Прямой доступ к Next.js будет заблокирован
- ✅ Атаки на порт 3000 не будут работать

