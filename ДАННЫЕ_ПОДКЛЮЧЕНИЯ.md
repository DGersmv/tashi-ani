# 🔐 Данные подключения к MySQL на reg.ru

## ✅ Полученные данные

- **Пользователь:** `u3269198_default`
- **Пароль:** `Gdv2210974!!!`
- **База данных:** `u3269198_default`
- **Хост:** `localhost` (или нужно уточнить в панели reg.ru)

## ⚙️ Текущая конфигурация

Файл `config.php` обновлен с вашими данными:

```php
define('DATABASE_URL', 'mysql://u3269198_default:Gdv2210974!!!@localhost:3306/u3269198_default');
```

## ⚠️ Важно

Если подключение не работает с `localhost`, проверьте в панели reg.ru:
- Может быть указан другой хост (например, IP адрес или доменное имя)
- Может быть другой порт (не 3306)

## 🔍 Где найти хост в панели reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в раздел "Базы данных MySQL"
3. Найдите базу `u3269198_default`
4. Там будет указан **хост** (может быть `localhost` или другой адрес)

## ✅ После получения правильного хоста

Обновите в `config.php`:
```php
define('DATABASE_URL', 'mysql://u3269198_default:Gdv2210974!!!@ПРАВИЛЬНЫЙ_ХОСТ:3306/u3269198_default');
```

Или если хост действительно `localhost`, то подключение должно работать на самом хостинге reg.ru.
