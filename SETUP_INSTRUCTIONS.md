# 🚀 Инструкции по настройке системы

## 1. Создайте файл `.env.local`

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
DATABASE_URL="file:./dev.db"
MASTER_ADMIN_EMAIL="2277277@bk.ru"
MASTER_ADMIN_PASSWORD="admin123"
JWT_SECRET="your-secret-key-here"
EMAIL_USER="0383877@gmail.com"
EMAIL_PASS="pvkdnsgmbfdiyywm"
```

## 2. Создайте базу данных

```bash
npx prisma generate
npx prisma db push
```

## 3. Создайте тестовые данные

```bash
node create-test-data.js
```

## 4. Запустите сервер

```bash
npm run dev
```

## 5. Откройте Prisma Studio (опционально)

```bash
npx prisma studio
```

## 🎯 Как работает система:

### Мастер-админ (2277277@bk.ru):
- Входит по паролю `admin123`
- Видит всех заказчиков в админ панели
- Может добавлять/удалять заказчиков

### Обычные пользователи:
- Входят по email + код с почты
- Видят только свои проекты в личном кабинете
- Не могут регистрироваться самостоятельно (только через админа)

## 📧 Настройка email:
- EMAIL_USER: ваш Gmail
- EMAIL_PASS: пароль приложения Gmail
- Письма будут отправляться с этого адреса