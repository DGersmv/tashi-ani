# SendGrid - БЕСПЛАТНО 100 писем в день!

## 🎉 РЕАЛЬНО РАБОТАЕТ!

### 1. Регистрация на SendGrid
- Заходим на https://sendgrid.com/
- Регистрируемся (бесплатно)
- Подтверждаем email
- Получаем API ключ

### 2. Настраиваем API
```javascript
const sgMail = require('@sendgrid/mail');
sgMail.setApiKey(process.env.SENDGRID_API_KEY);

const msg = {
  to: email,
  from: 'admin@tashi-ani.ru',
  subject: 'Код подтверждения',
  text: `Ваш код: ${code}`,
  html: `<strong>Ваш код: ${code}</strong>`
};

await sgMail.send(msg);
```

### 3. Обновляем .env.local
```env
SENDGRID_API_KEY=your-sendgrid-api-key
```

## ✅ Преимущества SendGrid:

- ✅ **БЕСПЛАТНО** - 100 писем в день
- ✅ **РАБОТАЕТ** - не блокируется VPS
- ✅ **НАДЕЖНО** - профессиональный сервис
- ✅ **БЫСТРО** - доставляется за секунды

## 💰 Стоимость: 0 рублей!

100 писем в день = 3000 писем в месяц БЕСПЛАТНО!
