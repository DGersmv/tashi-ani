# 📋 Что нужно загрузить на сервер

## ✅ Что уже загружено (видно в файловом менеджере)

- ✓ `index.html`
- ✓ `config.php`
- ✓ `.htaccess`
- ✓ `public/` (папка)
- ✓ `uploads/` (папка)
- ✓ `video/` (папка)
- ✓ `wasm/` (папка)

## ❌ Что ОТСУТСТВУЕТ (критично!)

### 1. Папка `api/` с PHP файлами

**Это самая важная папка!** Без неё API не будет работать.

В папке `api/` должны быть:
- `api/db.php`
- `api/auth/login.php`
- `api/auth/send-code.php`
- `api/auth/verify-code.php`
- `api/user/profile.php`
- `api/user/objects.php`
- `api/user/object-details.php`
- `api/admin/users.php`
- `api/admin/objects.php`
- И ещё ~20 PHP файлов

### 2. Папка `_next/` со статическими файлами Next.js

Без неё сайт может не работать полностью (не загрузятся JS/CSS файлы).

## 🔧 Как загрузить недостающие файлы

### Вариант 1: Через файловый менеджер ISPmanager

1. В файловом менеджере нажмите **"Загрузить"** (Upload)
2. Выберите папку `api/` из `E:\tashi-ani\dist\api`
3. Загрузите всю папку `api/`
4. Затем загрузите папку `_next/` из `E:\tashi-ani\dist\_next`

### Вариант 2: Через FileZilla

1. Откройте FileZilla
2. Подключитесь к серверу
3. Слева: откройте `E:\tashi-ani\dist`
4. Справа: перейдите в `/www/tashi-ani.ru/`
5. **Найдите папку `api/`** в `dist` и перетащите её на сервер
6. **Найдите папку `_next/`** в `dist` и перетащите её на сервер

## ✅ Проверка после загрузки

В файловом менеджере должны появиться:
- Папка `api/` с ~30 PHP файлами
- Папка `_next/` с JS/CSS файлами

## 🧪 Тест API

После загрузки `api/` проверьте:
```
http://tashi-ani.ru/api/portfolio.php
```

Должен вернуться JSON ответ.

---

**ГЛАВНОЕ: Загрузите папку `api/` - без неё API не работает!**
