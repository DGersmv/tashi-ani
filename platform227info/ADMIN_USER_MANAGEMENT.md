# Управление пользователями - Руководство администратора

## 🎯 Возможности админа

### 1. Добавление новых пользователей
- **Поля для заполнения:**
  - Email (обязательно)
  - Имя (необязательно)
  - Пароль (обязательно, минимум 6 символов)
  - Роль (Пользователь/Администратор)

### 2. Редактирование пользователей
- **Доступные поля:**
  - Email
  - Имя
  - Роль (Пользователь/Администратор)
  - Статус (Активен/Неактивен)
  - Новый пароль (опционально)
  - Дополнительная информация (JSON)

### 3. Управление паролями
- **Быстрый сброс пароля** - кнопка "Пароль" для каждого пользователя
- **Редактирование пароля** - через форму редактирования
- **Автоматическое хеширование** всех паролей

### 4. Просмотр деталей пользователей
- **Основная информация:** email, имя, роль, статус
- **Статистика:** количество объектов, сообщений, комментариев
- **Активность:** дата создания, последний вход
- **Дополнительные данные:** метаданные в JSON формате

## 🔧 Технические детали

### API Endpoints
- `GET /api/admin/users` - список всех пользователей
- `POST /api/admin/users/create` - создание нового пользователя
- `GET /api/admin/users/[id]` - детали пользователя
- `PUT /api/admin/users/[id]` - редактирование пользователя
- `POST /api/admin/users/[id]/reset-password` - сброс пароля
- `DELETE /api/admin/users` - удаление пользователя

### Безопасность
- JWT токены для авторизации
- Хеширование паролей с bcrypt
- Проверка ролей для доступа к админским функциям
- Валидация данных на клиенте и сервере

## 📋 Инструкция по использованию

### Добавление пользователя:
1. Заполните все обязательные поля (email, пароль)
2. Выберите роль пользователя
3. Нажмите "Добавить пользователя"

### Редактирование пользователя:
1. Нажмите "Редактировать" рядом с пользователем
2. Измените нужные поля
3. Для смены пароля введите новый пароль
4. Нажмите "Сохранить"

### Сброс пароля:
1. Нажмите "Пароль" рядом с пользователем
2. Введите новый пароль
3. Пароль будет изменен немедленно

### Просмотр деталей:
1. Нажмите "Детали" рядом с пользователем
2. Просмотрите полную информацию о пользователе
3. Закройте окно кнопкой "×"

## ⚠️ Важные замечания

- **Пароли обязательны** для всех пользователей
- **Минимальная длина пароля** - 6 символов
- **Админов нельзя удалять** - только обычных пользователей
- **Все изменения сохраняются** автоматически
- **Пароли хешируются** и не хранятся в открытом виде

