# 🚀 Быстрое исправление ошибки авторизации

## Выполните эти команды на сервере по порядку:

```bash
# 1. Подключитесь к серверу
ssh root@89.104.67.209

# 2. Перейдите в директорию проекта
cd /var/www/tashi-ani

# 3. Проверьте наличие .env.local
cat .env.local

# 4. Если файла нет или в нём нет JWT_SECRET, создайте/отредактируйте:
nano .env.local
```

## Вставьте в .env.local (замените значения на свои):

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-super-secret-jwt-key-change-this"
MASTER_ADMIN_EMAIL="2277277@bk.ru"
MASTER_ADMIN_PASSWORD="admin123"
EMAIL_USER="your-email@example.com"
EMAIL_PASS="your-email-password"
```

**Сохраните файл:** `Ctrl+X`, затем `Y`, затем `Enter`

## Продолжите выполнение команд:

```bash
# 5. Проверьте, что база данных существует
ls -la prisma/dev.db
# Или если база в корне проекта:
ls -la dev.db

# 6. Если базы нет, создайте её:
npx prisma migrate deploy

# 7. ВАЖНО! Создайте/обновите пользователя админа в базе данных:
node create-admin-user.js

# Этот скрипт создаст пользователя с email и паролем из .env.local
# Вы должны увидеть: "✅ Админ создан!" или "✅ Пароль админа обновлен!"

# 8. Перезапустите PM2 с обновлением переменных окружения
pm2 restart tashi-ani --update-env

# 9. Проверьте логи
pm2 logs tashi-ani --lines 30

# 10. Проверьте статус
pm2 status
```

## ✅ Готово!

Теперь попробуйте войти на сайт. Если ошибка осталась:

1. **Очистите localStorage в браузере:**
   - Нажмите F12
   - Application → Local Storage → очистите всё
   - Обновите страницу

2. **Проверьте, что JWT_SECRET установлен:**
   ```bash
   grep JWT_SECRET .env.local
   ```

3. **Посмотрите полные логи ошибок:**
   ```bash
   pm2 logs tashi-ani --err --lines 100
   ```

## 🔍 Частые проблемы:

### Проблема: "JWT_SECRET is not defined"
**Решение:** Добавьте `JWT_SECRET` в `.env.local` и перезапустите:
```bash
echo 'JWT_SECRET="your-secret-key-here"' >> .env.local
pm2 restart tashi-ani --update-env
```

### Проблема: "Database connection failed"
**Решение:** Проверьте путь к базе данных:
```bash
# Проверьте текущий DATABASE_URL
grep DATABASE_URL .env.local

# Если путь неправильный, исправьте:
# Должно быть: DATABASE_URL="file:./dev.db" (Prisma ищет относительно директории prisma)
```

### Проблема: "Неверный пароль и пользователь"
**Решение:** Пользователь не существует в базе данных. Создайте его:
```bash
node create-admin-user.js
```

### Проблема: Токены не работают
**Решение:** 
1. Убедитесь, что `JWT_SECRET` одинаковый везде
2. Выйдите и войдите заново
3. Очистите localStorage в браузере

