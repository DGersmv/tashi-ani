# 📋 План действий: Добавление функционала 3D моделей с комментариями

## 🎯 Цель
Добавить в проект tashi-ani полный функционал работы с 3D моделями (BIM) и комментариями к ним, как это реализовано в personal227info.

---

## 📦 Этап 1: Установка зависимостей

### Необходимые пакеты:
1. **@thatopen/components** - библиотека для работы с IFC моделями
2. **@thatopen/fragments** - работа с фрагментами 3D моделей
3. **@thatopen/ui** - UI компоненты (опционально)
4. **web-ifc** - парсер IFC файлов
5. **@types/three** - типы для TypeScript (если еще не установлены)

### Команда установки:
```bash
npm install @thatopen/components @thatopen/fragments @thatopen/ui web-ifc
npm install --save-dev @types/three
```

---

## 🗄️ Этап 2: Обновление схемы базы данных

### 2.1. Добавить enum для форматов моделей
В `prisma/schema.prisma` добавить:
```prisma
enum BimModelFormat {
  SKETCHUP    // SketchUp (.skp)
  REVIT       // Revit (.rvt)
  ARCHICAD    // ArchiCAD (.pln, .pla)
  IFC         // Industry Foundation Classes (.ifc)
  GLTF        // glTF (.gltf, .glb)
  OBJ         // Wavefront OBJ (.obj)
  THREE_DS    // 3D Studio (.3ds)
  OTHER       // Другие форматы
}
```

### 2.2. Добавить модель BimModel
Связь с Object, Project, ProjectStage (опционально)
- Исходный файл (обязательный)
- Файл для просмотра (опциональный - IFC/GLTF)
- Метаданные (название, описание, версия)
- Видимость для заказчика
- Автор загрузки

### 2.3. Добавить модель BimModelComment
- Связь с BimModel и User
- Координаты в 3D пространстве (x, y, z)
- Содержимое комментария
- Флаги видимости и прочтения

### 2.4. Обновить связи в существующих моделях
- User → добавить связь с BimModel и BimModelComment
- Object → добавить связь с BimModel
- Project → добавить связь с BimModel
- ProjectStage → добавить связь с BimModel

### 2.5. Создать миграцию
```bash
npx prisma migrate dev --name add_bim_models
```

---

## 🔌 Этап 3: Создание API endpoints

### 3.1. Основные endpoints для моделей

#### `GET /api/objects/[id]/models`
- Получение списка всех моделей объекта
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Сортировка по дате загрузки
- Возвращает все модели объекта (без фильтрации по видимости)

#### `POST /api/objects/[id]/models`
- Загрузка новой модели
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Обработка multipart/form-data
- Сохранение исходного файла
- Обработка файла для просмотра (если предоставлен)
- Валидация форматов
- Сохранение `uploadedByUserId` для проверки прав на удаление

#### `GET /api/objects/[id]/models/[modelId]`
- Получение информации о модели
- Метаданные, автор, даты

#### `DELETE /api/objects/[id]/models/[modelId]`
- Удаление модели
- Проверка прав (только автор или админ)
- Удаление файлов с диска

#### `GET /api/objects/[id]/models/[modelId]/view`
- Отдача файла для просмотра (IFC/GLTF)
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Правильные заголовки Content-Type
- Поддержка range requests для больших файлов

#### `GET /api/objects/[id]/models/[modelId]/download`
- Скачивание исходного файла
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Параметр `?type=original` или `?type=viewable`

### 3.2. Endpoints для комментариев

#### `GET /api/objects/[id]/models/[modelId]/comments`
- Получение всех комментариев к модели
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Сортировка по дате создания
- Возвращает все комментарии к модели (без фильтрации по видимости)

#### `POST /api/objects/[id]/models/[modelId]/comments`
- Создание нового комментария
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- Валидация координат (x, y, z)
- Сохранение автора комментария (`userId`)

#### `DELETE /api/objects/[id]/models/[modelId]/comments/[commentId]`
- Удаление комментария
- **Проверка доступа:** пользователь должен иметь доступ к объекту
- **Проверка прав:** только автор комментария или MASTER могут удалять

---

## 🎨 Этап 4: Создание React компонентов

### 4.1. BimModelUploader
**Путь:** `src/components/BimModelUploader.tsx`

**Функционал:**
- Форма загрузки модели
- Выбор исходного файла (обязательный)
- Выбор файла для просмотра (опциональный)
- Поля: название, описание, версия
- Чекбокс видимости для заказчика (опционально, для уведомлений/статистики)
- Валидация форматов файлов
- Индикатор загрузки
- **Примечание:** Поле `isVisibleToCustomer` не влияет на доступ к просмотру - все пользователи с доступом к объекту могут видеть все модели

### 4.2. BimModelsList
**Путь:** `src/components/BimModelsList.tsx`

**Функционал:**
- Список всех моделей объекта (для пользователей с доступом)
- Карточки моделей с превью информации
- Кнопка загрузки новой модели (для пользователей с доступом)
- Открытие просмотрщика по клику
- Отображение количества комментариев

### 4.3. BimModelViewer
**Путь:** `src/components/BimModelViewer.tsx`

**Функционал:**
- Просмотр IFC моделей через @thatopen/components
- Просмотр GLTF моделей через model-viewer
- Дерево параметров IFC (spatial structure)
- Управление видимостью элементов через дерево
- Система комментариев:
  - Режим добавления комментариев
  - Gizmo для точного позиционирования
  - Визуальные маркеры на модели
  - Панель со списком комментариев
  - Создание/удаление комментариев
- Кнопки скачивания файлов
- Кнопка удаления модели (для автора/админа)

**Особенности реализации:**
- Использование ThatOpen Components для IFC
- Three.js для визуализации и raycasting
- Gizmo с осями X, Y, Z для позиционирования
- Проецирование точек на поверхность модели
- Превью-маркер при движении мыши

---

## 📁 Этап 5: Структура файлов

### 5.1. Создать директории для хранения файлов
```
public/uploads/objects/[objectId]/models/
  ├── original/     # Исходные файлы
  ├── viewable/     # Файлы для просмотра (IFC/GLTF)
  └── thumbnails/   # Превью (если будут)
```

### 5.2. Добавить WASM файлы для IFC
```
public/wasm/
  ├── web-ifc-mt.wasm
  └── web-ifc.wasm
```

---

## 🔗 Этап 6: Интеграция в существующие компоненты

### 6.1. ObjectDetailView
- Добавить вкладку/секцию "3D Модели"
- Интегрировать BimModelsList
- Проверка прав на загрузку

### 6.2. Dashboard
- Добавить панель с последними моделями (опционально)
- Счетчик моделей

### 6.3. AdminObjectDetailView
- Добавить управление моделями в админ-панели
- Просмотр всех моделей независимо от видимости

---

## 🛠️ Этап 7: Обработка файлов

### 7.1. Валидация форматов
- Проверка расширений файлов
- Проверка MIME типов
- Ограничение размера файлов

### 7.2. Сохранение файлов
- Генерация уникальных имен файлов
- Сохранение оригинальных имен
- Расчет размера файлов
- Определение формата по расширению

### 7.3. Безопасность
- Проверка прав доступа
- Валидация путей (защита от path traversal)
- Ограничение типов файлов

---

## 🎯 Этап 8: Права доступа

### 8.1. Проверка доступа к объекту
**Основной принцип:** Доступ определяется через связь User-Object в базе данных.

**Проверка доступа:**
```typescript
// Проверяем, что пользователь имеет доступ к объекту
const user = await prisma.user.findUnique({
  where: { email },
  include: {
    objects: {
      where: { id: objectId }
    }
  }
});

if (!user || user.objects.length === 0) {
  return NextResponse.json({ 
    success: false, 
    message: 'Объект не найден или нет доступа' 
  }, { status: 404 });
}
```

### 8.2. Просмотр моделей
- **Все пользователи с доступом к объекту** могут просматривать все модели этого объекта
- Не требуется проверка `isVisibleToCustomer` для просмотра
- `isVisibleToCustomer` может использоваться для других целей (например, уведомления)

### 8.3. Комментирование моделей
- **Все пользователи с доступом к объекту** могут:
  - Создавать комментарии к любым моделям объекта
  - Просматривать все комментарии к моделям объекта
  - Удалять только свои комментарии (или все, если это MASTER)

### 8.4. Загрузка моделей
- **MASTER (админ)**: может загружать модели к любым объектам
- **USER**: может загружать модели только к объектам, к которым имеет доступ

### 8.5. Скачивание файлов
- **Все пользователи с доступом к объекту** могут скачивать файлы моделей
- MASTER может скачивать файлы любых моделей

### 8.6. Удаление моделей
- **MASTER**: может удалять любые модели
- **USER**: может удалять только модели, которые сам загрузил (проверка `uploadedByUserId`)

---

## 📝 Этап 9: Документация и тестирование

### 9.1. Обновить README
- Описание нового функционала
- Инструкции по использованию

### 9.2. Тестирование
- Загрузка моделей разных форматов
- Просмотр IFC моделей
- Просмотр GLTF моделей
- Создание комментариев
- Удаление комментариев
- **Проверка прав доступа:**
  - Пользователь с доступом к объекту может просматривать и комментировать
  - Пользователь без доступа к объекту получает ошибку 404
  - MASTER может работать со всеми объектами

---

## 🔄 Этап 10: Миграция данных (если нужно)

Если в базе уже есть данные:
- Создать скрипт миграции существующих данных
- Проверить целостность связей

---

## 📊 Приоритеты выполнения

### Высокий приоритет (MVP):
1. ✅ Установка зависимостей
2. ✅ Обновление схемы БД
3. ✅ Базовые API endpoints (CRUD для моделей)
4. ✅ Компонент BimModelUploader
5. ✅ Компонент BimModelsList
6. ✅ Базовый BimModelViewer (только просмотр)

### Средний приоритет:
7. ✅ API для комментариев
8. ✅ Система комментариев в BimModelViewer
9. ✅ Дерево параметров IFC
10. ✅ Gizmo для позиционирования

### Низкий приоритет (улучшения):
11. ⚪ Превью/миниатюры моделей
12. ⚪ Поиск по моделям
13. ⚪ Фильтрация по форматам
14. ⚪ Версионирование моделей
15. ⚪ История изменений

---

## ⚠️ Важные замечания

1. **SQLite vs PostgreSQL**: 
   - В tashi-ani используется SQLite
   - В personal227info используется PostgreSQL
   - Убедиться, что все типы данных совместимы

2. **Размер файлов**:
   - IFC файлы могут быть очень большими (100+ MB)
   - Настроить лимиты загрузки в Next.js
   - Рассмотреть возможность фоновой обработки

3. **Производительность**:
   - Кэширование загруженных моделей
   - Ленивая загрузка компонентов
   - Оптимизация рендеринга Three.js

4. **Браузерная совместимость**:
   - WebAssembly для IFC парсинга
   - Проверка поддержки в разных браузерах

5. **Безопасность**:
   - Валидация всех входных данных
   - Защита от XSS в комментариях
   - Ограничение доступа к файлам
   - **Проверка доступа к объекту во всех API endpoints:**
   ```typescript
   // Универсальная функция проверки доступа к объекту
   async function checkObjectAccess(email: string, objectId: number) {
     const user = await prisma.user.findUnique({
       where: { email },
       include: {
         objects: {
           where: { id: objectId }
         }
       }
     });
     
     if (!user || user.objects.length === 0) {
       throw new Error('Объект не найден или нет доступа');
     }
     
     return user;
   }
   
   // Использование в API endpoint:
   const user = await checkObjectAccess(email, objectId);
   // Теперь можно работать с моделями этого объекта
   ```

---

## 🚀 Готово к реализации!

После утверждения плана можно приступать к поэтапной реализации. Рекомендую начать с этапов 1-3 (зависимости, БД, API), затем перейти к компонентам.

