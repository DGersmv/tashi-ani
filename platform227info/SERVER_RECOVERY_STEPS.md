# 🔧 Пошаговая инструкция для сервера

## Шаг 1: Поиск старых баз данных (ВОССТАНОВЛЕНИЕ ДАННЫХ)

Выполните на сервере для поиска всех .db файлов:

```bash
# Поиск всех .db файлов на сервере
sudo find / -type f -name "*.db" 2>/dev/null | grep -v "/proc\|/sys\|/dev"

# Более детальный поиск в ключевых местах
echo "=== Поиск в /var/www/tashi-ani ==="
find /var/www/tashi-ani -name "*.db" 2>/dev/null

echo "=== Поиск в /var/lib ==="
find /var/lib -name "*.db" 2>/dev/null

echo "=== Поиск в /home/deploy ==="
find /home/deploy -name "*.db" 2>/dev/null

echo "=== Поиск в /tmp ==="
find /tmp -name "*.db" 2>/dev/null

echo "=== Поиск в /var/backups ==="
find /var/backups -name "*.db" 2>/dev/null

echo "=== Поиск в текущей директории ==="
find . -name "*.db" 2>/dev/null
```

**Если нашли старый .db файл:**
```bash
# Проверьте размер файла (должен быть больше 0)
ls -lh /путь/к/старому/файлу.db

# Проверьте содержимое (должны быть таблицы)
sqlite3 /путь/к/старому/файлу.db ".tables"

# Если есть данные - делаем дамп
sqlite3 /путь/к/старому/файлу.db ".dump" > /tmp/old_db_dump.sql

# Восстанавливаем в новую БД
sqlite3 /var/lib/tashi-ani/db/tashi-ani.db < /tmp/old_db_dump.sql

# Проверяем что восстановилось
sqlite3 /var/lib/tashi-ani/db/tashi-ani.db ".tables"
```

---

## Шаг 2: Проверка текущего состояния

```bash
cd /var/www/tashi-ani

# Проверьте текущую БД
ls -lh /var/lib/tashi-ani/db/tashi-ani.db

# Проверьте таблицы в текущей БД
sqlite3 /var/lib/tashi-ani/db/tashi-ani.db ".tables"

# Проверьте количество пользователей
sqlite3 /var/lib/tashi-ani/db/tashi-ani.db "SELECT COUNT(*) FROM User;"

# Проверьте переменные окружения
cat .env | grep DATABASE_URL
```

---

## Шаг 3: Обновление кода с последними исправлениями безопасности

```bash
cd /var/www/tashi-ani

# 1. Сохраните текущее состояние (на всякий случай)
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 2. Обновите код
git pull origin master

# 3. Установите зависимости (если были изменения)
npm install

# 4. Примените миграции (если есть новые)
npx prisma migrate deploy

# 5. Пересоберите приложение
npm run build

# 6. Перезапустите приложение
pm2 restart tashi-ani

# 7. Проверьте логи
pm2 logs tashi-ani --lines 50
```

---

## Шаг 4: Настройка автобэкапа базы данных

### Создайте скрипт для бэкапа:

```bash
sudo mkdir -p /var/backups/tashi-ani
sudo nano /var/backups/tashi-ani/backup-db.sh
```

**Содержимое скрипта:**
```bash
#!/bin/bash
# Скрипт автоматического бэкапа базы данных

DB_PATH="/var/lib/tashi-ani/db/tashi-ani.db"
BACKUP_DIR="/var/backups/tashi-ani"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/db-${DATE}.sqlite"

# Создаем директорию если не существует
mkdir -p "${BACKUP_DIR}"

# Делаем бэкап
sqlite3 "${DB_PATH}" ".backup '${BACKUP_FILE}'"

# Удаляем старые бэкапы (оставляем последние 30 дней)
find "${BACKUP_DIR}" -name "db-*.sqlite" -mtime +30 -delete

# Логируем
echo "$(date): Backup created: ${BACKUP_FILE}" >> "${BACKUP_DIR}/backup.log"

# Проверяем размер бэкапа
if [ -f "${BACKUP_FILE}" ]; then
    SIZE=$(du -h "${BACKUP_FILE}" | cut -f1)
    echo "$(date): Backup size: ${SIZE}" >> "${BACKUP_DIR}/backup.log"
fi
```

**Сделайте скрипт исполняемым:**
```bash
sudo chmod +x /var/backups/tashi-ani/backup-db.sh
sudo chown deploy:deploy /var/backups/tashi-ani/backup-db.sh
```

**Добавьте в cron (ежедневный бэкап в 3:00 ночи):**
```bash
sudo crontab -e
```

**Добавьте строку:**
```
0 3 * * * /var/backups/tashi-ani/backup-db.sh
```

**Или для пользователя deploy:**
```bash
crontab -e
```

**Добавьте строку:**
```
0 3 * * * /var/backups/tashi-ani/backup-db.sh
```

**Проверьте что cron работает:**
```bash
# Запустите вручную для проверки
/var/backups/tashi-ani/backup-db.sh

# Проверьте что файл создался
ls -lh /var/backups/tashi-ani/
```

---

## Шаг 5: Проверка безопасности

```bash
# 1. Проверьте что Next.js слушает только локально
sudo netstat -tulpn | grep :3000
# Должно быть: 127.0.0.1:3000 или ::1:3000

# 2. Проверьте конфигурацию nginx
sudo nginx -t

# 3. Проверьте что nginx блокирует неправильные домены
sudo cat /etc/nginx/sites-available/tashi-ani | grep server_name
# Должно быть: server_name tashi-ani.ru www.tashi-ani.ru;

# 4. Проверьте логи на подозрительную активность
pm2 logs tashi-ani --lines 100 | grep -E "SECURITY ALERT"

# 5. Проверьте что нет подозрительных процессов
ps aux | grep -E "(boatnet|yamaha|broncano)"
```

---

## Шаг 6: Проверка работы приложения

```bash
# 1. Проверьте статус PM2
pm2 status

# 2. Проверьте что приложение отвечает
curl -I http://127.0.0.1:3000

# 3. Проверьте через nginx
curl -I https://tashi-ani.ru

# 4. Проверьте логи в реальном времени
pm2 logs tashi-ani --lines 20
```

---

## Шаг 7: Создание пользователя-админа (если данные потеряны)

Если данные не удалось восстановить, создайте нового админа:

```bash
cd /var/www/tashi-ani
node create-admin-user.js
```

Или вручную через Prisma Studio:
```bash
npx prisma studio
# Откройте http://localhost:5555 в браузере
# Создайте пользователя в таблице User
```

---

## Шаг 8: Проверка файлов загрузок

Проверьте что физические файлы (фото, панорамы) на месте:

```bash
# Проверьте структуру директорий
ls -la /var/www/tashi-ani/public/uploads/

# Проверьте объекты
ls -la /var/www/tashi-ani/public/uploads/objects/

# Проверьте размеры (если файлы есть)
du -sh /var/www/tashi-ani/public/uploads/objects/*

# Если файлы есть, но не привязаны к БД - можно восстановить частично
# (но это требует ручной работы)
```

---

## ⚠️ Важные замечания

1. **Бэкапы**: Настройте автобэкап СЕЙЧАС, до того как что-то еще случится
2. **Мониторинг**: Проверяйте логи регулярно на `SECURITY ALERT`
3. **Обновления**: Регулярно делайте `git pull` и обновляйте зависимости
4. **S3**: В будущем рассмотрите перенос медиа-файлов в S3

---

## 📞 Если что-то пошло не так

1. Проверьте логи: `pm2 logs tashi-ani`
2. Проверьте статус: `pm2 status`
3. Проверьте nginx: `sudo nginx -t`
4. Проверьте права доступа: `ls -la /var/lib/tashi-ani/db/`

