# ✅ Проверка файла .env

## 📝 Ваша строка подключения

```
DATABASE_URL="postgresql://admin:Gdv2210974!@79.174.89.232:15555/tashi_ani_db?sslmode=require"
```

## ⚠️ ВАЖНО: Проверить существует ли БД `tashi_ani_db`

**Мы видели в списке БД только:**
- `admin`
- `db1`

**НЕ видели БД `tashi_ani_db`!**

---

## 🔍 Что нужно сделать

### Вариант А: Если БД `tashi_ani_db` существует (но не отобразилась в списке)

**Строка подключения правильная!** Можно использовать.

### Вариант Б: Если БД `tashi_ani_db` НЕ существует

**Нужно изменить имя БД на существующую:**

**Использовать `db1` (рекомендуется):**
```
DATABASE_URL="postgresql://admin:Gdv2210974!@79.174.89.232:15555/db1?sslmode=require"
```

**Или использовать `admin`:**
```
DATABASE_URL="postgresql://admin:Gdv2210974!@79.174.89.232:15555/admin?sslmode=require"
```

---

## ✅ Проверка формата строки

**Ваша строка правильная по формату:**
- ✅ Правильный формат: `postgresql://логин:пароль@хост:порт/база?sslmode=require`
- ✅ Логин: `admin` ✓
- ✅ Пароль: `Gdv2210974!` ✓
- ✅ Хост: `79.174.89.232` ✓
- ✅ Порт: `15555` ✓
- ⚠️ База: `tashi_ani_db` - нужно проверить существует ли!

---

## 🚀 Как проверить существует ли БД

**Подключитесь к postgres и посмотрите список:**

```powershell
$env:PGPASSWORD="Gdv2210974!"
& "C:\Program Files\PostgreSQL\17\bin\psql.exe" -h 79.174.89.232 -p 15555 -U admin -d postgres
```

**В psql:**
```sql
\l  # Показать все БД
```

**Ищите `tashi_ani_db` в списке!**

---

## 📝 Рекомендация

**Если БД `tashi_ani_db` НЕ существует:**

1. **Используйте БД `db1`** (она точно существует):
   ```
   DATABASE_URL="postgresql://admin:Gdv2210974!@79.174.89.232:15555/db1?sslmode=require"
   ```

2. **Или обратитесь в поддержку reg.ru** для создания БД `tashi_ani_db`

---

## ✅ Если строка правильная

**После проверки что БД существует, можете продолжать:**

```powershell
npx prisma generate
npx prisma migrate deploy
```
