# Обновление .env.local на сервере

## 🚀 Обновляем переменные окружения на сервере

### 1. Подключаемся к серверу
```bash
ssh root@89.104.67.209
```

### 2. Переходим в директорию проекта
```bash
cd /var/www/tashi-ani
```

### 3. Редактируем .env.local
```bash
nano .env.local
```

### 4. Обновляем содержимое файла
```env
# Email настройки для Yandex
EMAIL_USER=tashi-ani@ya.ru
EMAIL_PASS=Admin123!!!

# Мастер-админ
MASTER_ADMIN_EMAIL=2277277@bk.ru
MASTER_ADMIN_PASSWORD=admin123

# JWT секрет
JWT_SECRET=your-super-secret-jwt-key-here

# База данных
DATABASE_URL="file:./dev.db"
```

### 5. Сохраняем файл
- Нажимаем `Ctrl + X`
- Нажимаем `Y` для подтверждения
- Нажимаем `Enter`

### 6. Перезапускаем приложение
```bash
pm2 restart tashi-ani
```

### 7. Проверяем логи
```bash
pm2 logs tashi-ani
```

## ✅ Что изменилось:

- **EMAIL_USER**: `tashi-ani@ya.ru` (ваша новая Yandex почта)
- **EMAIL_PASS**: `Admin123!!!` (ваш пароль)

## 🔧 Код уже обновлен!

Код автоматически определит, что это Yandex почта (`@ya.ru`) и будет использовать:
- **Хост**: `smtp.yandex.ru`
- **Порт**: `587`
- **Шифрование**: STARTTLS

## 🚀 Тестирование:

1. Обновите .env.local на сервере
2. Перезапустите PM2
3. Протестируйте отправку кода на сайте
4. Проверьте логи на ошибки

## 💰 Стоимость: 0 рублей!

Yandex почта полностью бесплатная!
