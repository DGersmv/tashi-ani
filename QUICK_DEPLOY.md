# 🚀 Быстрый деплой на reg.ru

## ✅ Что уже готово:
- ✅ Синтаксическая ошибка исправлена
- ✅ Проект собран и готов к деплою
- ✅ Созданы все необходимые файлы конфигурации

## 📋 Пошаговая инструкция:

### 1. Сборка проекта (на вашем компьютере)
```bash
npm run build
```

### 2. Создание архива для загрузки
Создайте ZIP архив со всеми файлами проекта, исключив:
- `node_modules/`
- `.next/`
- `.git/`
- `*.log`
- `.env.local`
- `prisma/dev.db`

### 3. Загрузка на сервер reg.ru

#### Через панель управления reg.ru:
1. Зайдите в панель управления хостингом
2. Откройте файловый менеджер
3. Загрузите ZIP архив в корневую директорию сайта
4. Распакуйте архив

#### Через SSH (если доступен):
```bash
# Загрузите файлы на сервер
scp -r . user@your-server:/var/www/html/

# Подключитесь к серверу
ssh user@your-server

# Перейдите в директорию проекта
cd /var/www/html/

# Установите зависимости
npm install

# Соберите проект
npm run build
```

### 4. Настройка базы данных

#### Если у вас есть доступ к PostgreSQL:
```bash
# Создайте базу данных
createdb tashi_ani_prod

# Примените миграции
npx prisma migrate deploy
```

#### Если используете SQLite (проще для начала):
```bash
# Скопируйте файл базы данных
cp prisma/dev.db prisma/prod.db

# Обновите DATABASE_URL в .env
DATABASE_URL="file:./prisma/prod.db"
```

### 5. Настройка переменных окружения

Создайте файл `.env.local` на сервере:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/tashi_ani_prod"
# или для SQLite:
# DATABASE_URL="file:./prisma/prod.db"

NEXTAUTH_SECRET="your-super-secret-key-here"
NEXTAUTH_URL="https://yourdomain.ru"

# Email настройки
EMAIL_HOST="smtp.reg.ru"
EMAIL_PORT=587
EMAIL_USER="your-email@yourdomain.ru"
EMAIL_PASS="your-email-password"

# Админ
MASTER_ADMIN_EMAIL="admin@yourdomain.ru"
MASTER_ADMIN_PASSWORD="your-secure-password"
```

### 6. Запуск приложения

#### Вариант 1: Через PM2 (рекомендуется)
```bash
# Установите PM2
npm install -g pm2

# Запустите приложение
npm run pm2:start

# Сохраните конфигурацию
pm2 save
pm2 startup
```

#### Вариант 2: Обычный запуск
```bash
npm start
```

### 7. Настройка домена

1. В панели reg.ru настройте домен на директорию с проектом
2. Убедитесь, что порт 3000 доступен
3. При необходимости настройте Nginx для проксирования

## 🔧 Проверка работы

1. Откройте браузер и перейдите на ваш домен
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Авторизация работает
   - ✅ Админ-панель доступна
   - ✅ Загрузка файлов работает
   - ✅ PDF просмотр работает

## 🆘 Если что-то не работает

### Проверьте логи:
```bash
# PM2 логи
npm run pm2:logs

# Обычные логи
npm start
```

### Частые проблемы:
1. **База данных не подключена** - проверьте DATABASE_URL
2. **Порт занят** - измените PORT в .env
3. **Файлы не загружаются** - проверьте права доступа к папке uploads
4. **Email не отправляется** - проверьте настройки SMTP

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи приложения
2. Убедитесь, что все переменные окружения настроены
3. Проверьте права доступа к файлам и папкам

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу вашего домена!

