# 🔧 Исправление ошибки авторизации на сервере

## Проблема
После обновления кода появилась ошибка авторизации. Это происходит потому, что PM2 не всегда правильно загружает переменные окружения из `.env.local`.

## ✅ Пошаговое решение

### Шаг 1: Подключитесь к серверу
```bash
ssh root@89.104.67.209
```

### Шаг 2: Перейдите в директорию проекта
```bash
cd /var/www/tashi-ani
```

### Шаг 3: Проверьте наличие файла .env.local
```bash
ls -la .env.local
```

Если файла нет, создайте его:
```bash
nano .env.local
```

### Шаг 4: Убедитесь, что в .env.local есть ВСЕ необходимые переменные

Скопируйте и вставьте в файл (замените значения на свои реальные):

```env
# База данных (важно: путь должен быть правильным!)
# Prisma ищет файл относительно директории prisma, поэтому используем file:./dev.db
DATABASE_URL="file:./dev.db"

# JWT секрет (ОБЯЗАТЕЛЬНО! Должен быть одинаковым везде)
JWT_SECRET="your-super-secret-jwt-key-change-this-to-random-string"

# Мастер-админ
MASTER_ADMIN_EMAIL="2277277@bk.ru"
MASTER_ADMIN_PASSWORD="admin123"

# Email настройки (используйте свои реальные данные)
EMAIL_USER="your-email@example.com"
EMAIL_PASS="your-email-password"
```

**ВАЖНО:** 
- `JWT_SECRET` должен быть **одинаковым** на сервере и в локальной разработке
- `DATABASE_URL` должен указывать на правильный путь к базе данных
- Если используете SQLite, путь должен быть `file:./prisma/dev.db` или `file:./prisma/prod.db`

### Шаг 5: Сохраните файл
- Нажмите `Ctrl + X`
- Нажмите `Y` для подтверждения
- Нажмите `Enter`

### Шаг 6: Проверьте путь к базе данных
```bash
ls -la prisma/dev.db
```

Если файла нет, создайте его:
```bash
npx prisma migrate deploy
# или
npx prisma db push
```

### Шаг 7: Перезапустите PM2 с обновлением переменных окружения
```bash
pm2 restart tashi-ani --update-env
```

**ВАЖНО:** Флаг `--update-env` обновляет переменные окружения из `.env.local`

### Шаг 8: Проверьте логи
```bash
pm2 logs tashi-ani --lines 50
```

Ищите ошибки, связанные с:
- `JWT_SECRET` - должен быть установлен
- `DATABASE_URL` - должен быть правильным
- Авторизация - проверьте, что токены валидны

### Шаг 9: Проверьте статус приложения
```bash
pm2 status
```

Должно быть `online`

### Шаг 10: Проверьте работу сайта
1. Откройте сайт в браузере
2. Попробуйте войти как админ
3. Проверьте, что авторизация работает

## 🔍 Диагностика проблем

### Если ошибка "JWT_SECRET is not defined":
```bash
# Проверьте, что JWT_SECRET есть в .env.local
cat .env.local | grep JWT_SECRET

# Если нет, добавьте:
echo 'JWT_SECRET="your-secret-key-here"' >> .env.local
pm2 restart tashi-ani --update-env
```

### Если ошибка "Database connection failed":
```bash
# Проверьте путь к базе данных
cat .env.local | grep DATABASE_URL

# Проверьте, существует ли файл базы данных
ls -la prisma/dev.db

# Если нет, создайте:
npx prisma migrate deploy
```

### Если токены не работают:
1. Убедитесь, что `JWT_SECRET` одинаковый везде
2. Выйдите из системы и войдите заново
3. Очистите localStorage в браузере (F12 -> Application -> Local Storage -> Clear)

## 📝 Дополнительные команды

### Просмотр всех переменных окружения процесса PM2:
```bash
pm2 env 0
```

### Полная перезагрузка PM2:
```bash
pm2 delete tashi-ani
pm2 start ecosystem.config.js
pm2 save
```

### Проверка переменных окружения в Node.js:
```bash
node -e "console.log(process.env.JWT_SECRET)"
```

## ✅ Чеклист успешной настройки

- [ ] Файл `.env.local` существует на сервере
- [ ] В `.env.local` есть `JWT_SECRET` с реальным значением
- [ ] В `.env.local` есть `DATABASE_URL` с правильным путем
- [ ] В `.env.local` есть `MASTER_ADMIN_EMAIL` и `MASTER_ADMIN_PASSWORD`
- [ ] База данных существует по указанному пути
- [ ] PM2 перезапущен с флагом `--update-env`
- [ ] Приложение работает (`pm2 status` показывает `online`)
- [ ] Логи не показывают ошибок (`pm2 logs tashi-ani`)
- [ ] Авторизация работает на сайте

## 🆘 Если ничего не помогает

1. **Полностью остановите и перезапустите приложение:**
```bash
pm2 delete tashi-ani
cd /var/www/tashi-ani
npm run build
pm2 start ecosystem.config.js
pm2 save
```

2. **Проверьте, что все зависимости установлены:**
```bash
npm install
```

3. **Проверьте версию Node.js:**
```bash
node --version
# Должна быть 18.x или выше
```

4. **Посмотрите полные логи:**
```bash
pm2 logs tashi-ani --lines 100 --err
```

